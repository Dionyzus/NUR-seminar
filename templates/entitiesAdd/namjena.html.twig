{% extends 'base.html.twig' %}
{% block title %}Nova namjena{% endblock %}
{% block body %}
    <br>
    <div class="row">
        <div class="col-6 offset-3">
            <div class="card bg-light">
                <div class="card-body">
                    <h1 class="h3 mb-3 font-weight-normal text-center">Dodaj novu namjenu</h1>
                    {{ form_start(form) }}
                    {{ form_row(form.nazivNamjene) }}
                    {{ form_row(form.opisNamjene) }}
                    <button class="btn btn-primary btn-block">Dodaj</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}